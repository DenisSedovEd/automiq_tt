[project]
name = "automiq-tt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "jinja2>=3.1.6",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
]
